<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#e1e7ef</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="QuestionTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#fbf7da</item>
    </style>
    <style name="subItemTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#e1e7ef</item>
    </style>
    <style name="MainItemTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">#dde7f2</item>
    </style><style name="AboutTheme" parent="Theme.AppCompat">
    <!-- Customize your theme here. -->
    <item name="colorPrimary">@color/colorPrimary</item>
</style>

    <style name="button_style" parent="@style/Widget.AppCompat.Button.Borderless">
        <item name="android:gravity">center_vertical|center_horizontal</item>
        <item name="android:background">@drawable/main_button_style</item>
        <item name="android:focusable">true</item>
        <item name="android:clickable">true</item>
        <item name="android:layout_marginBottom">5dp</item>
        <item name="android:layout_marginTop">5dp</item>
    </style>
</resources>
